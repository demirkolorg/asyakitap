generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model User {
  id                String                  @id @default(uuid())
  email             String                  @unique
  books             Book[]
  challengeProgress UserChallengeProgress[]
  createdAt         DateTime                @default(now())
  updatedAt         DateTime                @updatedAt
}

enum BookStatus {
  TO_READ
  READING
  COMPLETED
  DNF
}

// ==========================================
// Yazar Modeli
// ==========================================

model Author {
  id        String   @id @default(cuid())
  name      String
  imageUrl  String?
  bio       String?  @db.Text
  books     Book[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([name])
}

// ==========================================
// Yayınevi Modeli
// ==========================================

model Publisher {
  id        String   @id @default(cuid())
  name      String
  imageUrl  String?
  website   String?
  books     Book[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([name])
}

model Book {
  id                   String                @id @default(cuid())
  userId               String
  user                 User                  @relation(fields: [userId], references: [id], onDelete: Cascade)
  title                String
  authorId             String?
  author               Author?               @relation(fields: [authorId], references: [id])
  publisherId          String?
  publisher            Publisher?            @relation(fields: [publisherId], references: [id])
  coverUrl             String?
  pageCount            Int?
  isbn                 String?
  publishedDate        String?               // Yayın tarihi (örn: "29.10.2025")
  description          String?               @db.Text // Kitap açıklaması
  inLibrary            Boolean               @default(false) // Kitap fiziksel olarak kütüphanede mi?
  status               BookStatus            @default(TO_READ)
  currentPage          Int                   @default(0)
  tortu                String?               @db.Text // Kitaptan aklında kalanlar
  tortuAiComment       String?               @db.Text // AI'ın tortu yorumu
  imza                 String?               @db.Text // Yazarın bu kitaptaki üslubu/tarzı
  imzaAiComment        String?               @db.Text // AI'ın imza yorumu
  quotes               Quote[]
  readingLogs          ReadingLog[]
  aiComments           AIComment[]           // AI yorumları
  readingListBooks     ReadingListBook[]     // Bu kitabın bulunduğu okuma listeleri
  challengeBooks       ChallengeBook[]       // Bu kitabın bulunduğu challenge'lar
  rating               BookRating?           // Kitap puanlaması (1-1 ilişki)
  startDate            DateTime?
  endDate              DateTime?
  readingGoalDays      Int?                  // Kullanıcının hedeflediği okuma gün sayısı
  createdAt            DateTime              @default(now())
  updatedAt            DateTime              @updatedAt

  @@index([userId])
  @@index([authorId])
  @@index([publisherId])
  @@index([userId, status]) // Status bazlı sorgular için (reading, completed, etc.)
  @@index([userId, inLibrary]) // Kütüphanedeki kitaplar için
  @@index([userId, updatedAt]) // Son güncellenen kitaplar için
}

enum ReadingAction {
  STARTED
  FINISHED
  ABANDONED
  RESTARTED
  ADDED_TO_LIST
}

// ==========================================
// AI Yorumları
// ==========================================

enum AICommentSource {
  TORTU
  IMZA
  STATS  // İstatistik analizi
}

model AIComment {
  id          String           @id @default(cuid())
  bookId      String?          // Opsiyonel - STATS için null olabilir
  book        Book?            @relation(fields: [bookId], references: [id], onDelete: Cascade)
  userId      String?          // STATS için kullanıcı ID'si
  source      AICommentSource  // TORTU, IMZA veya STATS
  userContent String           @db.Text // Kullanıcının yazdığı orijinal içerik (STATS için istatistik özeti)
  aiComment   String           @db.Text // AI'ın yorumu
  createdAt   DateTime         @default(now())
  updatedAt   DateTime         @updatedAt

  @@index([bookId])
  @@index([userId])
  @@index([source])
  @@index([createdAt])
}

model ReadingLog {
  id        String        @id @default(cuid())
  bookId    String
  book      Book          @relation(fields: [bookId], references: [id], onDelete: Cascade)
  action    ReadingAction
  note      String?
  createdAt DateTime      @default(now())

  @@index([bookId])
  @@index([bookId, createdAt]) // Sıralı okuma logları için
}

model Quote {
  id        String   @id @default(cuid())
  bookId    String
  book      Book     @relation(fields: [bookId], references: [id], onDelete: Cascade)
  content   String   @db.Text
  page      Int?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([bookId])
  @@index([bookId, createdAt]) // Sıralı alıntılar için
}

// ==========================================
// Kitap Puanlama Sistemi
// ==========================================

model BookRating {
  id              String   @id @default(cuid())
  bookId          String   @unique // Her kitapta sadece bir puanlama olabilir
  book            Book     @relation(fields: [bookId], references: [id], onDelete: Cascade)

  // İçerik & Hikaye (1-10)
  konuFikir       Int      // Konu/Fikir: Ana tema, mesaj veya fikrin özgünlüğü ve derinliği
  akicilik        Int      // Akıcılık: Okuma hızı, sayfa çevirme isteği
  derinlik        Int      // Derinlik: Konunun işlenme seviyesi, detay zenginliği
  etki            Int      // Etki: Kitabın bıraktığı iz, düşündürme gücü

  // Yazarlık & Üslup (1-10)
  dilUslup        Int      // Dil & Üslup: Yazarın anlatım gücü, kelime seçimi
  karakterAnlatim Int      // Karakter/Anlatım: Karakter derinliği veya anlatım tutarlılığı
  ozgunluk        Int      // Özgünlük: Yazarın kendine has sesi

  // Teknik & Üretim (1-10)
  baskiTasarim    Int      // Baskı/Tasarım: Kapak, kağıt kalitesi, punto, sayfa düzeni

  // Genel (1-10)
  tavsiyeEderim   Int      @default(5) // Tavsiye Eder misin?: Başkalarına önerir misin? (1-10)
  genelPuan       Int      // Genel Puan: Tüm değerlendirmelerin özeti

  // Hesaplanan ortalama (otomatik)
  ortalamaPuan    Float    // 10 kategorinin ortalaması

  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  @@index([bookId])
  @@index([ortalamaPuan])
}

// ==========================================
// Tematik Okuma Listeleri
// ==========================================

model ReadingList {
  id          String             @id @default(cuid())
  slug        String             @unique
  name        String
  description String?            @db.Text
  coverUrl    String?
  sortOrder   Int                @default(0)
  levels      ReadingListLevel[]
  createdAt   DateTime           @default(now())
  updatedAt   DateTime           @updatedAt
}

model ReadingListLevel {
  id            String            @id @default(cuid())
  readingListId String
  readingList   ReadingList       @relation(fields: [readingListId], references: [id], onDelete: Cascade)
  levelNumber   Int
  name          String
  description   String?           @db.Text
  coverUrl      String?           // Seviye kapak görseli
  books         ReadingListBook[]
  createdAt     DateTime          @default(now())
  updatedAt     DateTime          @updatedAt

  @@unique([readingListId, levelNumber])
  @@index([readingListId])
}

model ReadingListBook {
  id        String                @id @default(cuid())
  levelId   String
  level     ReadingListLevel      @relation(fields: [levelId], references: [id], onDelete: Cascade)
  bookId    String                // Book tablosuna referans
  book      Book                  @relation(fields: [bookId], references: [id], onDelete: Cascade)
  neden     String?               @db.Text // Bu kitap neden okunmalı?
  sortOrder Int                   @default(0)
  createdAt DateTime              @default(now())
  updatedAt DateTime              @updatedAt

  @@unique([levelId, bookId]) // Aynı seviyede aynı kitap olamaz
  @@index([levelId])
  @@index([bookId])
}


// ==========================================
// Yıllık Okuma Hedefi (Reading Challenge)
// ==========================================

model ReadingChallenge {
  id          String                 @id @default(cuid())
  year        Int                    @unique
  name        String                 // "2026 Okuma Hedefi"
  description String?                @db.Text
  strategy    String                 @default("1_MAIN_2_BONUS")
  isActive    Boolean                @default(true)
  months      ChallengeMonth[]
  userProgress UserChallengeProgress[]
  createdAt   DateTime               @default(now())
  updatedAt   DateTime               @updatedAt
}

model ChallengeMonth {
  id          String            @id @default(cuid())
  challengeId String
  challenge   ReadingChallenge  @relation(fields: [challengeId], references: [id], onDelete: Cascade)
  monthNumber Int               // 1-12
  monthName   String            // "Ocak", "Şubat"...
  theme       String            // "Bilim Kurgu & Macera"
  themeIcon   String?           // "🚀"
  books       ChallengeBook[]
  createdAt   DateTime          @default(now())
  updatedAt   DateTime          @updatedAt

  @@unique([challengeId, monthNumber])
  @@index([challengeId])
}

enum ChallengeBookRole {
  MAIN
  BONUS
}

enum ChallengeBookStatus {
  LOCKED      // Bonus kitaplar başlangıçta kilitli
  NOT_STARTED // Erişilebilir ama başlanmamış
  IN_PROGRESS // Okunuyor
  COMPLETED   // Tamamlandı
}

model ChallengeBook {
  id           String              @id @default(cuid())
  monthId      String
  month        ChallengeMonth      @relation(fields: [monthId], references: [id], onDelete: Cascade)
  bookId       String              // Book tablosuna referans
  book         Book                @relation(fields: [bookId], references: [id], onDelete: Cascade)
  role         ChallengeBookRole   // MAIN veya BONUS
  reason       String?             @db.Text // Neden bu kitap seçildi
  sortOrder    Int                 @default(0)
  userProgress UserChallengeBook[]
  createdAt    DateTime            @default(now())
  updatedAt    DateTime            @updatedAt

  @@unique([monthId, bookId]) // Aynı ayda aynı kitap olamaz
  @@index([monthId])
  @@index([monthId, role])
  @@index([bookId])
}

// Kullanıcının challenge genelindeki durumu
model UserChallengeProgress {
  id           String           @id @default(cuid())
  userId       String
  user         User             @relation(fields: [userId], references: [id], onDelete: Cascade)
  challengeId  String
  challenge    ReadingChallenge @relation(fields: [challengeId], references: [id], onDelete: Cascade)
  joinedAt     DateTime         @default(now())
  books        UserChallengeBook[]

  @@unique([userId, challengeId])
  @@index([userId])
  @@index([challengeId])
}

// Kullanıcının her kitap için durumu
model UserChallengeBook {
  id              String               @id @default(cuid())
  userProgressId  String
  userProgress    UserChallengeProgress @relation(fields: [userProgressId], references: [id], onDelete: Cascade)
  challengeBookId String
  challengeBook   ChallengeBook        @relation(fields: [challengeBookId], references: [id], onDelete: Cascade)
  status          ChallengeBookStatus  @default(LOCKED)
  startedAt       DateTime?
  completedAt     DateTime?
  takeaway        String?              @db.Text // "Bu kitaptan aklında kalan tek cümle"
  createdAt       DateTime             @default(now())
  updatedAt       DateTime             @updatedAt

  @@unique([userProgressId, challengeBookId])
  @@index([userProgressId])
  @@index([challengeBookId])
  @@index([status])
}
